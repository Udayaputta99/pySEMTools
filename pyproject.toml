[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "extremeflow-pysemtools"
version = "1.4.0.dev0"
readme = "README.md"
requires-python = ">= 3.10"
dependencies = [
    "numpy",
    "scipy",
    "pymech",
    "tqdm",
    "mpi4py",
]

[project.urls]
Repository = "https://github.com/ExtremeFLOW/pySEMTools.git"
Issues = "https://github.com/ExtremeFLOW/pySEMTools/issues"

[project.optional-dependencies]
cpu = [
    "pytest",
    "pytest-json-report",
    "pympler",
    "memory_profiler",
    "h5py",
    "rtree",
    "jupyter", 
    "matplotlib",
    "pyevtk",
]
test = [
    "extremeflow-pysemtools[torch]",
    "pytest",
    "pytest-json-report",
    "pympler",
    "memory_profiler",
    "h5py",
    "rtree",
    "jupyter", 
    "matplotlib",
    "pyevtk",
]
dev = [
    "pre-commit",
]
torch = [
    "torch",
    "torchvision",
    "torchaudio"
]
all = [
    "extremeflow-pysemtools[test]",
    "extremeflow-pysemtools[dev]"
]

[project.scripts]
pysemtools_index-files = "pysemtools.cli.index_files:main"
pysemtools_visnek = "pysemtools.cli.visnek:main"
pysemtools_extract_subdomain = "pysemtools.cli.extract_subdomain:main"
